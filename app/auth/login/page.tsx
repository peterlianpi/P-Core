/**
 * Login Page - Using Existing Auth Components
 *
 * Authentication page using the existing NextAuth-based login form
 */

import { LoginForm } from '@/components/auth/login-form';

export default function LoginPage() {
  return <LoginForm />;
}
