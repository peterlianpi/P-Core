/**
 * Register Page - Using Existing Auth Components
 *
 * User registration page using the existing NextAuth-based register form
 */

import { RegisterForm } from '@/components/auth/register-form';

export default function RegisterPage() {
  return <RegisterForm />;
}
